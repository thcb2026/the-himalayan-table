# Deprecated: the Worker handles SPA fallback directly.
# Cloudflare Pages redirect rules are intentionally disabled here to avoid
# redirect loops when the worker serves the app bundle and the assets fallback.
